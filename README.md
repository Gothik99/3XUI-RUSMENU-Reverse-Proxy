# Скрипт обновления меню x-ui

## Описание

Скрипт `update_xui_menu.sh` автоматически обновляет меню x-ui из репозитория [Gothik99/3XUI-RUSMENU-Reverse-Proxy](https://github.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy).

## Возможности

- ✅ Автоматическое создание резервной копии текущего меню
- ✅ Проверка доступности репозитория перед загрузкой
- ✅ Проверка синтаксиса загруженного файла
- ✅ Автоматическое восстановление из резервной копии при ошибке
- ✅ Обновление меню в `/usr/bin/x-ui` и `/usr/local/x-ui/x-ui.sh`

## Использование

### 1. Загрузка скрипта

```bash
wget -O update_xui_menu.sh https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/update_xui_menu.sh
chmod +x update_xui_menu.sh
```

### 2. Запуск скрипта

```bash
sudo bash update_xui_menu.sh
```

или

```bash
sudo ./update_xui_menu.sh
```

### 3. Применение изменений

После успешного обновления перезапустите меню x-ui:

```bash
x-ui
```

## Откат изменений

Если что-то пошло не так, вы можете восстановить предыдущую версию из резервной копии:

```bash
cp /tmp/x-ui-backup-YYYYMMDD-HHMMSS/x-ui.backup /usr/bin/x-ui
chmod +x /usr/bin/x-ui
```

Где `YYYYMMDD-HHMMSS` - дата и время создания резервной копии.

## Требования

- Linux система с установленным x-ui
- Права root (sudo)
- Интернет-соединение для загрузки файла из GitHub
- Утилиты: `wget`, `curl`, `bash`

## Безопасность

Скрипт выполняет следующие проверки безопасности:

1. Проверка прав root перед выполнением
2. Проверка существования текущего меню x-ui
3. Проверка доступности репозитория
4. Проверка синтаксиса загруженного файла перед установкой
5. Автоматическое создание резервной копии

## URL репозитория

```
https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/x-ui.sh
```

## Пример вывода

```
====================================================
   Обновление меню x-ui из репозитория Gothik99     
====================================================

[INF] Создание резервной копии текущего меню...
[INF] Резервная копия сохранена в: /tmp/x-ui-backup-20250101-120000/x-ui.backup
[INF] Загрузка обновленного меню из репозитория...
[DEG] URL: https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/x-ui.sh
[INF] Проверка синтаксиса загруженного файла...
[INF] Установка нового меню...
[INF] Обновление /usr/local/x-ui/x-ui.sh...
[INF] Обновление успешно завершено!

════════════════════════════════════════════════
   Меню x-ui успешно обновлено!
════════════════════════════════════════════════

Резервная копия: /tmp/x-ui-backup-20250101-120000/x-ui.backup
Для отката выполните: cp /tmp/x-ui-backup-20250101-120000/x-ui.backup /usr/bin/x-ui

Для применения изменений перезапустите скрипт: x-ui
```

## Автор

Gothik99

## Лицензия

См. лицензию репозитория [Gothik99/3XUI-RUSMENU-Reverse-Proxy](https://github.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy)


Этот скрипт обновляет меню управления 3X-UI на версию из репозитория [Gothik99/3XUI-RUSMENU-Reverse-Proxy](https://github.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy).

## 📋 Описание

Скрипт `update-menu-gothik.sh` автоматически:
- Создает резервную копию текущего меню
- Загружает обновленную версию из репозитория Gothik99
- Устанавливает новое меню в `/usr/bin/x-ui` и `/usr/local/x-ui/x-ui.sh`
- Делает файлы исполняемыми

## 🚀 Использование

### Вариант 1: Прямая загрузка и запуск

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/update-menu-gothik.sh)
```

### Вариант 2: Скачать и запустить локально

```bash
# Скачать скрипт
wget https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/update-menu-gothik.sh

# Сделать исполняемым
chmod +x update-menu-gothik.sh

# Запустить
sudo ./update-menu-gothik.sh
```

## ⚙️ Требования

- Root доступ (sudo)
- Интернет-соединение
- Утилиты: `curl`, `wget`, `bash`

## 📝 Что делает скрипт

1. **Проверка прав root** - убеждается, что скрипт запущен от имени root
2. **Создание резервной копии** - сохраняет текущие версии меню в `/tmp/x-ui-backup-YYYYMMDD-HHMMSS/`
3. **Проверка доступности репозитория** - проверяет, что GitHub доступен
4. **Загрузка нового меню** - скачивает файл из репозитория
5. **Валидация файла** - проверяет, что загруженный файл является валидным bash скриптом
6. **Установка** - копирует файл в нужные места и делает исполняемым
7. **Очистка** - удаляет временные файлы

## 🔄 После обновления

После успешного обновления вы можете запустить меню командой:

```bash
x-ui
```

## ⚠️ Важные замечания

- Скрипт создает резервные копии перед обновлением
- Если что-то пойдет не так, вы можете восстановить старую версию из резервной копии
- Резервные копии хранятся в `/tmp/` и могут быть удалены при перезагрузке

## 🐛 Устранение неполадок

### Ошибка: "Не удалось подключиться к репозиторию"
- Проверьте интернет-соединение
- Убедитесь, что GitHub доступен с вашего сервера
- Проверьте настройки файрвола

### Ошибка: "Не удалось загрузить файл меню"
- Проверьте доступность URL: https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/x-ui.sh
- Убедитесь, что у вас достаточно места на диске

### Ошибка прав доступа
- Убедитесь, что скрипт запущен от имени root: `sudo ./update-menu-gothik.sh`

## 📦 Восстановление из резервной копии

Если нужно восстановить предыдущую версию меню:

```bash
# Найти резервную копию
ls -la /tmp/x-ui-backup-*

# Восстановить
cp /tmp/x-ui-backup-YYYYMMDD-HHMMSS/x-ui-backup.sh /usr/bin/x-ui
chmod +x /usr/bin/x-ui
```

## 🔗 Ссылки

- Репозиторий: https://github.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy
- Файл меню: https://raw.githubusercontent.com/Gothik99/3XUI-RUSMENU-Reverse-Proxy/main/x-ui.sh

